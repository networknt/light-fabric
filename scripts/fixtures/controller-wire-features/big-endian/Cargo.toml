[package]
name = "controller-wire-big-endian-fixture"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
controller-wire = { path = "../../../../crates/controller-wire" }
rkyv = { version = "=0.8.12", default-features = false, features = ["big_endian"] }
