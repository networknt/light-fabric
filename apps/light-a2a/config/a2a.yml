runtimePolicy:
  publicationId: ${runtimePolicy.publicationId:}
  releaseVersion: ${runtimePolicy.releaseVersion:0}
  policySnapshotId: ${runtimePolicy.policySnapshotId:}
  policyVersion: ${runtimePolicy.policyVersion:0}
  policyDigest: ${runtimePolicy.policyDigest:}
  audience: ${runtimePolicy.audience:light-a2a}
  host: ${runtimePolicy.host:}
  serviceId: ${runtimePolicy.serviceId:com.networknt.light-a2a-1.0.0}
  envTag: ${runtimePolicy.envTag:dev}
  contentDigest: ${runtimePolicy.contentDigest:}
  sourceEventSequence: ${runtimePolicy.sourceEventSequence:0}
  schemaVersion: ${runtimePolicy.schemaVersion:1}
  createdAt: ${runtimePolicy.createdAt:}
  validFrom: ${runtimePolicy.validFrom:}
  refreshAfter: ${runtimePolicy.refreshAfter:}
  expiresAt: ${runtimePolicy.expiresAt:}
  revocationEpoch: ${runtimePolicy.revocationEpoch:0}
  compatibilityGeneration: ${runtimePolicy.compatibilityGeneration:1}
operationalStore:
  contractVersion: ${operationalStore.contractVersion:2}
  bindingId: ${operationalStore.bindingId:}
  bindingDigest: ${operationalStore.bindingDigest:}
  hostId: ${operationalStore.hostId:}
  environment: ${operationalStore.environment:dev}
  serverHost: ${operationalStore.serverHost:postgres}
  port: ${operationalStore.port:5432}
  tlsMode: ${operationalStore.tlsMode:DISABLE}
  serviceOwner: ${operationalStore.serviceOwner:light-a2a}
  schema: ${operationalStore.schema:a2a_ops}
  expectedDatabase: ${operationalStore.expectedDatabase:operations}
  minimumSchemaGeneration: ${operationalStore.minimumSchemaGeneration:2}
  databaseUrlFile: ${operationalStore.databaseUrlFile:/run/secrets/operational-database-url}
  credentialGeneration: ${operationalStore.credentialGeneration:1}
managedArtifactStore:
  bindingId: ${artifactStore.bindingId:}
  bindingDigest: ${artifactStore.bindingDigest:}
  minimumSchemaGeneration: ${artifactStore.minimumSchemaGeneration:1}
  databaseUrlFile: ${artifactStore.databaseUrlFile:/run/secrets/artifact-database-url}
  rootDirectory: ${artifactStore.rootDirectory:/var/lib/light-a2a/artifacts}
  scanProfileId: ${artifactStore.scanProfileId:light-a2a-static-v1}
  allowedMediaTypes: ${artifactStore.allowedMediaTypes:[text/plain,application/json,application/pdf,image/png,image/jpeg]}
authorizationContextKeyFile: ${a2aPolicy.authorizationContextKeyFile:/run/secrets/a2a-authorized-context-key}
maximumDatabaseConnections: ${a2aPolicy.maximumDatabaseConnections:16}
maximumRequestBytes: ${a2aPolicy.maximumRequestBytes:1048576}
maximumResponseBytes: ${a2aPolicy.maximumResponseBytes:4194304}
requestTimeoutMs: ${a2aPolicy.requestTimeoutMs:120000}
allowUnsignedAgentCards: ${a2aPolicy.allowUnsignedAgentCards:false}
bindings: ${a2aPolicy.bindings:[]}
