serviceId: ${server.serviceId:com.networknt.light-a2a-1.0.0}
environment: ${server.environment:dev}
envTag: ${startup.envTag:dev}
hostId: ${runtimePolicy.hostId:}
instanceId: ${runtimePolicy.instanceId:}
configServer:
  enabled: ${configServer.enabled:false}
  url: ${configServer.url:}
  pathPrefix: ${configServer.pathPrefix:/config}
  token: ${configServer.token:}
  timeoutMs: ${configServer.timeoutMs:5000}
  caFile: ${configServer.caFile:}
  certFile: ${configServer.certFile:}
  keyFile: ${configServer.keyFile:}
