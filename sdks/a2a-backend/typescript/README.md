# TypeScript/Node.js backend SDK

Implement `AgentBackend` and call `serve`. The package owns the loopback-only
listener, signed-context and restart-safe replay validation, bounded body,
fixed routes, identifier equality checks, health endpoints, and SSE framing.
